angular.module('app.controllers', [
    'app.globals'
])

.controller('home', function ($scope, _) {
    'use strict';

    $scope._ = _;
})

.controller('login', function ($scope, _) {
    'use strict';

    $scope._ = _;
});
